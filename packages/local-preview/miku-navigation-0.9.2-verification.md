# Miku 导航 0.9.2

设置标题栏清除宿主独立白底，使用下方设置背景。普通本地侧栏展开时，标题栏背景层按 --te-menu-width 延续侧栏 88% 底色；流媒体/设置/玻璃播放状态不添加本地侧栏色块。标题按钮沿用主题文字与悬停色，关闭悬停为红色。不更改拖动区、窗口事件、尺寸或层级。

- 使用宿主 TitleBar.vue 样式和 base.css 的模拟组件：深浅色 × 216/164/72px × 收起/展开/设置/流媒体，共 24 组通过。
- 检查设置背景透明、展开色块宽度、按钮尺寸不变、点击和关闭悬停通过。等待悬停过渡完成后检查颜色。
- 43 文件包完整性和源字节检查通过。
- test:themes 255 通过、1 失败；test:plugins 389 通过、1 失败、1 跳过，仍为既有宿主失败。
- 截图为模拟组件。尚未在实际 Electron 客户端安装和测试窗口拖动。
