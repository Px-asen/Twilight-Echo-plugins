# Miku 导航 0.2.0 验证记录

验证日期：2026-09-15。

本版接入用户提供的 9 个设置导航和 8 个流媒体导航图标，加上已有的 13 个主导航图标，共 30 张。基础视觉尺寸为 28px，保留宿主的按钮高度、文字位置及导航交互。

## 通过的检查

- 主导航实际组件：深浅色、3 种导航形态、3 种图标风格，共 18 种组合。
- 设置导航模板与流媒体实际组件：深浅色、1280px / 900px 窗口，共 8 种组合。
- 30 张图片可正常解码，均为带透明通道的 96×96 PNG。
- 替换前后按钮和文字几何位置一致，主导航按钮 40px、设置按钮 44px、流媒体按钮 42px。
- 菜单点击、流媒体 Enter / Space 操作、音源可用性切换、未配置图标保留以及关闭主题后原图标恢复。
- 浏览器页面错误为 0。
- 安装包清单通过宿主打包器校验；归档完整性、图片数量、内嵌图片与源文件一致性通过。
- 主仓库 `pnpm run lint`、`pnpm run typecheck` 通过；外部主题源码已按宿主 Prettier 配置定向格式化。

## 主仓库回归结果

| 检查                    | 结果                     | 失败原因                                                                                                                                          |
| ----------------------- | ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pnpm run test:plugins` | 389 通过、1 失败、1 跳过 | `src/main/security/ipcValidation.test.ts:201` 仍要求歌词文件在 IPC 源码中内联读取；工作区已有的歌词改动已将读取移到独立模块                       |
| `pnpm run test:themes`  | 255 通过、1 失败         | 现有硬编码颜色基线不接受 CommandPalette、LibraryLoudnessDialog、QueueWorkspaceDialog、AudioDeviceProfileEditor、AudioDeviceProfilesPanel 中的颜色 |

这两项失败属于宿主工作区，不由本版的外部图标 CSS 引入。本次未修改宿主源码或放宽测试基线。

## 安装包

- 文件：`com.twilightecho.theme.miku-navigation-0.2.0.tep`
- 字节数：831961
- SHA-256：`b183f80965ad1e55b138bce48218f39f460878f9f25c37e3edd5dd2822b77ae6`
- 包含 35 个文件：30 张 PNG，以及 manifest、CSS、README、许可和素材说明。
- 运行时脚本：0。构建和验证脚本不进入安装包。

以上为组件预览与归档验证，没有执行完整客户端中的端到端安装，也未改变用户当前启用的主题。流媒体预览使用模拟音源数据。

原始检查记录位于外部插件仓库的 `.cache/miku-navigation-preview/`；组件截图及 JSON 结果位于其 `rendered-v0.2.0/` 目录。
