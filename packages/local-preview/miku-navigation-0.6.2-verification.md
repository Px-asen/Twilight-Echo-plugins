# 0.6.2 固定人物背景验证

2026-09-15。修改仅在外部插件中。

人物由 `.song-list:has(.track-table-wrapper)` 的背景绘制，使用 scroll attachment：
固定在滚动容器自身，内部内容滚动不改变背景位置。表格背景透明，移除其背景模糊。
保持右侧定位、四周渐隐及原有内容布局，不新增监听器或脚本。

无头 Edge 验证使用宿主 base.css、SongList.css、主题令牌与模拟长表格，保留
主内容 translateZ(0)、表格变换和真正的列表内部滚动容器。

- 浅色/深色 × 2048/1440/900px × scrollTop 400/1800/7000，共 18 组。
- 临时隐藏文字层，图像解码及绘制完成后比较背景区域的解码像素，全部完全一致。
- 恢复文字层后歌曲行可点击，表格背景计算值为 none。
- 37 文件安装包 ZIP 完整性通过，全部文件与源码字节一致。
- 修改 CSS 使用宿主 Prettier 配置格式化。

测试为真实样式加模拟内容，未运行完整客户端安装与真实音频播放。
本轮未修改宿主虚拟滚动逻辑；既有主仓库测试问题参见 0.5.0 验证记录。
