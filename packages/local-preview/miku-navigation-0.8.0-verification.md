# Miku 导航 0.8.0 验证

新增 settings.css，仅作用于 settings-preview-layout。统一小按钮、输入框、分段按钮、开关、滑块、设置分组及设备/DSP 面板的主题配色与蒙版；不增加运行时脚本、素材或宿主修改。

- 使用宿主 base.css 和 SettingsPage.css 的控件模拟预览：深浅色 × 1440/850 窗口共 4 组通过。
- 检查控件宽高在启用/停用主题时不变；开关切换、禁用按钮、下拉选择、数值输入、滑块键盘操作、焦点描边与横向边界通过。
- 人工查看深浅色预览图；图片是组件模拟，不是实际客户端截图。
- 修改源码使用宿主 Prettier 配置格式化，构建与 38 文件安装包完整性/源文件逐字节检查通过。
- test:themes：255 通过、1 失败（宿主硬编码颜色基线）。
- test:plugins：389 通过、1 失败、1 跳过（宿主歌词 IPC 旧源码匹配）。两项均为之前已存在的宿主失败。
- 尚未在实际客户端安装验证。
